# Agent Handoff Prompt

你正在接手一个任务。请先读取任务票据，再执行。

交接要求：

1. 确认任务编号、状态、Owner 和 Lock。
2. 如果你不是 Owner，只能提供辅助产物，不要修改主产物。
3. 执行前写清你要做的步骤。
4. 执行后回填产出、验证结果和阻塞。
5. 完成后把状态改为 `REVIEW` 或 `DONE`。

不要做：

- 不要扩大任务范围。
- 不要修改未锁定文件。
- 不要跳过验收标准。

